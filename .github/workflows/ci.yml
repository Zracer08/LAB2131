name: Python CI
[cite_start]on: [push]
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: { python-version: '3.10' }
      - [cite_start]run: pip install -r requirements.txt
      - [cite_start]run: pytest